{
  "name": "link-app-render",
  "version": "1.0.0",
  "private": true,
  "description": "Link app web service for Render Free.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --check server.js"
  },
  "dependencies": {
    "pg": "^8.12.0"
  }
}
